//     Выведите на экран все файлы в папке var
# 6. Выводим на экран все файлы в папке var
    # Переходим в каталог var
        cd ../
    # grep ищет рекурсивно во всех файлах подкаталогов каталога var пустую строку и выводит содержимое на экран
        grep -r "" ./