//     Найдите в файле access.log все упоминания строки "404"
# 2. Находим в файле access.log все упоминания строки "404"
    # Переходим из папки html в папку logs
        cd ../../logs/
    # Находим с помощью grep строку "404" и считываем с помощью wc
        grep "404" access.log | wc -l